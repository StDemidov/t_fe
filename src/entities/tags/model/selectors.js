export const selectTags = (state) => state.tags;

export const selectTagsMain = (state) => state.tags.main;

export const selectTagsCloth = (state) => state.tags.cloth;

export const selectTagsOthers = (state) => state.tags.others;

export const selectTagsIsLoading = (state) => state.tags.isLoading;

export const selectTagsError = (state) => state.tags.error;
